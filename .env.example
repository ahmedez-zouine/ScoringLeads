# Copy this file to .env and fill in the values if needed.
# This project works out of the box without any .env file.

# Optional: override the API URL used by the Streamlit dashboard when running locally
# API_URL=http://localhost:8000
